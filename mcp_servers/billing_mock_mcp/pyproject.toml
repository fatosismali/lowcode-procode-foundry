[project]
name = "billing-mock-mcp"
version = "0.1.0"
description = "Mock MCP server for Vodafone care-billing APIs (getBillingProfiles, getMonthBillSummary, getPreviousBills, getSubscriptionDetails)"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "uvicorn>=0.30.0",
]

[tool.setuptools]
py-modules = ["server"]
